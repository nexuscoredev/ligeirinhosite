---
description: Ligeirinho Parceiros — complemento às User Rules globais
alwaysApply: true
---

# Ligeirinho Parceiros (ligeirinhosite)

Complementa as **User Rules globais** com o específico deste repo.

## Projeto

- Repo: `C:\dev\Ligeirinho\ligeirinhosite`
- GitHub: https://github.com/nexuscoredev/ligeirinhosite
- Deploy: https://ligeirinhoparceiros.vercel.app (Vercel `prj_ujXd63XhpVhq62hZ0L9mrkL9lzgB`, team `team_rIF5nQ4kiddAUYnHt6Fzv3c6`)
- Supabase: `ligeirinhoparceiros` (`tugbsnjyvfhyvtivfhea`)
- Site estático — `npm run build` é no-op

## Git deste repo

- Sempre **main** — sem PR, branch ou merge
- Antes de codar: `git pull origin main` · se `package-lock.json` mudou → `npm install`
- Ao concluir implementação: `git status` + `git diff` → pull --rebase → commit → push
- Push dispara deploy Vercel; confirmar via MCP ou `npx vercel --prod --yes`
- Nunca commitar `.env`, `data/auth-config.local.json`, segredos, `_tmp-*`
- Código só via GitHub — não OneDrive/Drive
- Conflito: parar e avisar

Commit + push **automáticos** ao concluir tarefa (ver `acoes-pre-aprovadas.mdc`) — não esperar pedido explícito.

## Checks antes do push

- `npm run migrate:check` (sem credencial DB → avisar, não bloquear)
- Pedido/checkout/auth/API → `npm run verify:prod`
- JS/CSS visível → bump `CACHE_NAME` em `js/sw.js`

## Regressão / piloto

- Feature nova: testar em `45028186000125` (distribuidora) antes de geral
- Mudou sync com Hub (pedido, cliente, `_shared/`) → validar também `ligeirinhohub`
- Depois do push: testar 1 fluxo crítico; front PWA → Ctrl+F5

## Relacionado

- Hub é repo/deploy **separado**: `C:\dev\Ligeirinho\ligeirinhohub`
